@model CityInformation.Models.grad

@{
    ViewBag.Title = "Brisanje grada";
    Layout = "~/Views/Shared/LayoutAdmin.cshtml";
}

<br />
<br />
<h2 class="text-center">Brisanje grada iz baze</h2>

@using (Html.BeginForm("DeleteGrad", "Administrator", FormMethod.Post))
{
    @Html.AntiForgeryToken()

    @Html.HiddenFor(model => model.idGrad)
    <hr />
    <h2 class="text-center">Jeste li sigurni da želite obrisati ovaj grad iz baze?</h2>
    <center>
        <h3>Ime županije: @Model.županija.imeŽupanija</h3>
        <h3>Ime grada: @Model.imeGrad</h3>
    </center>
    <br />
    <div class="form-group">
        <div class="col-md-12 text-center">
            <input type="submit" value="Potvrdi brisanje" class="btn btn-primary" />
        </div>
    </div>

}

<div>
    @Html.ActionLink("Natrag", "GlavniMeni")
</div>
